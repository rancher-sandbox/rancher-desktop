# nerdctl-sub/generate

This directory contains a tool that generates the argument parser for
nerdctl-stub (by parsing the output of `nerdctl -help`).

## Usage

```powershell
yarn generate:nerdctl-stub
```
